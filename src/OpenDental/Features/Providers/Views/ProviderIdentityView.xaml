<Window x:Class="OpenDental.Features.Providers.Views.ProviderIdentityView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:viewModels="clr-namespace:OpenDental.Features.Providers.ViewModels"
        mc:Ignorable="d" d:DataContext="{d:DesignInstance viewModels:ProviderIdentityViewModel}"
        ResizeMode="NoResize" WindowStartupLocation="CenterOwner"
        Title="Edit Provider Identification" Height="260" Width="380">

    <Grid Margin="5">
        <Grid.RowDefinitions>
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <Grid Grid.Row="0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>

            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>

            <TextBlock
                Grid.Row="0" Grid.Column="0"
                Padding="3" Text="Payor (Electronic) ID"
                VerticalAlignment="Center"
                TextAlignment="Right" />

            <TextBox
                Grid.Row="0" Grid.Column="1"
                Margin="3" Padding="2"
                VerticalContentAlignment="Center"
                Text="{Binding PayorId}" />

            <TextBlock
                Grid.Row="1" Grid.Column="0"
                Padding="3" Text="Type"
                TextAlignment="Right" />

            <ListBox
                Grid.Row="1" Grid.Column="1"
                Height="100" Margin="3"
                ItemsSource="{Binding Types}"
                SelectedItem="{Binding Type}" />

            <TextBlock
                Grid.Row="2" Grid.Column="0"
                Padding="3" Text="Assigned ID Number"
                VerticalAlignment="Center"
                TextAlignment="Right" />

            <TextBox
                Grid.Row="2" Grid.Column="1"
                Margin="3" Padding="2"
                VerticalContentAlignment="Center"
                Text="{Binding Value}" />
        </Grid>

        <StackPanel Grid.Row="1">
            <Button HorizontalAlignment="Right"
                    Width="80" Height="25"
                    Content="_Save"
                    Command="{Binding SaveCommand}" />
        </StackPanel>
    </Grid>

</Window>