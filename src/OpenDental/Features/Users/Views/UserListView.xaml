<UserControl x:Class="OpenDental.Features.Users.Views.UserListView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d" d:DesignHeight="400" d:DesignWidth="980">
    <Grid Margin="5">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="320" />
        </Grid.ColumnDefinitions>

        <Grid Grid.Column="0">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="*" />
            </Grid.RowDefinitions>

            <GroupBox Grid.Row="0" Header="User Filters" Padding="3">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="*" />
                    </Grid.ColumnDefinitions>

                    <Grid.RowDefinitions>
                        <RowDefinition Height="*" />
                        <RowDefinition Height="*" />
                    </Grid.RowDefinitions>

                    <StackPanel Grid.Row="0" Grid.Column="0" Margin="0 0 0 5">
                        <TextBlock Text="Show Only" Margin="0 2" />
                        <ComboBox Height="25" />
                    </StackPanel>

                    <StackPanel Grid.Row="0" Grid.Column="1" Margin="5 0 0 5">
                        <TextBlock Text="Group" Margin="0 2" />
                        <ComboBox Height="25" />
                    </StackPanel>

                    <StackPanel Grid.Row="0" Grid.Column="2" Margin="5 0 0 5">
                        <TextBlock Text="Clinic" Margin="0 2" />
                        <ComboBox Height="25" />
                    </StackPanel>

                    <StackPanel Grid.Row="1" Grid.Column="0">
                        <TextBlock Text="Clinic" Margin="0 2" />
                        <TextBox Height="25" VerticalContentAlignment="Center" />
                    </StackPanel>

                    <CheckBox
                        Grid.Row="1" Grid.Column="2"
                        Content="Show hidden users"
                        VerticalAlignment="Bottom"
                        Margin="5 0 0 5" />
                </Grid>
            </GroupBox>

            <Grid Grid.Row="1" Margin="0 10 0 0">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="160" />
                </Grid.ColumnDefinitions>

                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="*" />
                </Grid.RowDefinitions>

                <DataGrid Grid.Row="0" Grid.RowSpan="2" Grid.Column="0" Margin="0 0 5 0">
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="User" Width="*" />
                        <DataGridTextColumn Header="Employee" Width="*" />
                        <DataGridTextColumn Header="Provider" Width="*" />
                        <DataGridTextColumn Header="Clinic" Width="*" />
                    </DataGrid.Columns>
                </DataGrid>

                <StackPanel Grid.Row="0" Grid.Column="1">
                    <Button Content="Add User" Height="25" Margin="0 0 0 2" />
                    <Button Content="Copy User" Height="25" Margin="0 0 0 5" />

                    <TextBlock Text="User groups for" />
                    <TextBlock Text="None" Margin="0 0 0 5" />
                </StackPanel>

                <ListBox Grid.Row="1" Grid.Column="1" />
            </Grid>
        </Grid>

        <Grid Grid.Column="1" Margin="10 0 0 0">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="*" />
            </Grid.RowDefinitions>

            <TextBlock Margin="0 0 0 5">
                Effective permissions for user
            </TextBlock>

            <TreeView Grid.Row="1" VerticalAlignment="Stretch" />
        </Grid>
    </Grid>
</UserControl>